export const SNAPSHOT_INTERVAL_MS = 10 * 60 * 1000;

export const USD = "USD";
export const USD_DECIMALS = 3;
export const INITIAL_BALANCE = 5000;

export const LIQUIDATION_RATIO = Object.freeze({
    numerator: 19,
    denominator: 20,
});